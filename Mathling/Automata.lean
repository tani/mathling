    module

    public import Mathling.Automata.Core
    public import Mathling.Automata.Pushdown
    public import Mathling.Automata.Regex
    public import Mathling.Automata.Regular

    import LiterateLean
    open scoped LiterateLean


# Mathling Automata

有限オートマトン、正規表現、プッシュダウン・オートマトン、および正規言語の主要定理の領域入口である。公開下位モジュールは `Core`、`Regex`、`Pushdown`、`Regular` の四つであり、利用者は通常この入口だけを import する。

```lean
```

<!--
vim: set filetype=markdown :
Local Variables:
mode: markdown
End:
-->
